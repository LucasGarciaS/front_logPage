//Refresh Code

    function refreshNow() {
      location.reload(true);
  }

    var timeleft = 30;
    setInterval(function (){
    timeleft--;
    document.getElementById("countdowntimer").textContent = timeleft;
    if(timeleft <= 0)
        refreshNow();
    },1000);

